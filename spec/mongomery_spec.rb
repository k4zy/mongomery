require 'spec_helper'

describe Mongomery do
  it 'has a version number' do
    expect(Mongomery::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
